kind: Service
apiVersion: v1
metadata:
  name: boy-external
  annotations:
   service.beta.kubernetes.io/azure-load-balancer-internal: "true"
spec:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  selector:
    app: girl
  type: LoadBalancer